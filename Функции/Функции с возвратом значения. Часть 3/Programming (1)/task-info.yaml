type: code
custom_name: Programming
files:
- name: task.py
  visible: true
  text: |-
    # объявление функции
    def get_circle(radius):
        pass

    # считываем данные
    r = float(input())

    # вызываем функцию
    length, square = get_circle(r)
    print(length, square)
  learner_created: false
status: Unchecked
record: -1
